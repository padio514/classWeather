import { Button } from "bootstrap";
import React from "react";
const WeatherButton = ({cities}) => {
    const searchByCity = (cityName) =>{
        setCity(cityName);
        let url = ''
    }
    return (
        <div>
            <Button variant = "danger">현재 위치</Button>{' '}
        </div>
    )
}
export default WeatherButton